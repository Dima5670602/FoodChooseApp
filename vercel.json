{
  "version": 2,
  "builds": [{
    "src": "api/index.js",
    "use": "@vercel/node",
    "config": {
      "maxDuration": 30,
      "includeFiles": ["public/**"]
    }
  }],
  "routes": [{ "src": "/(.*)", "dest": "api/index.js" }]
}
